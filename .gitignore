# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# Pipeline data — never commit raw or intermediate data
pipeline/raw/snapshots/
pipeline/intermediate/
pipeline/output/

# Profiles werden committed (klein, dokumentarisch wertvoll)
# pipeline/profiles/  — NICHT ignorieren

# Old paths (legacy, sollten leer sein)
pipeline/raw/*.csv
pipeline/raw/*.xlsx
pipeline/raw/*.zip

# Environment files (Tokens, API-Keys)
.env
.env.local
*.env

# Output artefacts
*.parquet.tmp
.zip_tmp