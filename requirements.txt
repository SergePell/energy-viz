# Python-Abhängigkeiten der Datenpipeline (energy-viz)
# Für volle Reproduzierbarkeit die exakten Versionen der Arbeitsumgebung
# per `pip freeze > requirements.txt` festschreiben.

pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
statsmodels>=0.14
holidays>=0.40
matplotlib>=3.8
pyarrow>=15.0
requests>=2.31
